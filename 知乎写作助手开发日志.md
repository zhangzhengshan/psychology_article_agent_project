# 知乎心理科普文章撰写助手

## 2026/5/4
### 事件
因对心理学感兴趣，常在知乎进行心理学内容创作，但从取材到成文花费时间过多，计划开发一个Agent助手快速生成文章初稿。了解到Agent项目中固定流程编排架构实用且易上手，适合该轻量级小项目，故希望LLM生成一版项目规划。
### 问题
LLM给出的设计包含许多冗余文件，难以把握项目全貌。
### 解决
向LLM询问了让轻量级项目变复杂的关键词，并将这些关键词作为否定项加入生成规划的关键词中。
### 问题
LLM后续给出的版本虽轻量，但运用了很多国内基本不用的技术栈。
### 解决
意识到所使用的LLM主要基于英文语料，导致技术选型与国内不适配，因此加强提示词，强制要求仅使用国内技术栈。

## 2026/5/5
### 事件
完成llm.py文件并测试通过，可正常调用deepseek，同时完成search.py文件。
### 问题
使用DuckDuckGoSearchAPIWrapper时，重写了检索结果限制，要求一次性返回五个检索结果，但实际无论如何改写，都仅固定返回三个。
### 解决
发现DuckDuckGoSearchAPIWrapper的改写设置无法作用于DuckDuckGoSearch，导致修改前者不影响后者的检索结果，因此更换了工具包。

## 2026/5/6
### 事件
完成refine.py、outline.py、draft.py三个文件。
### 问题
refine.py文件测试时一直无法通过，显示DeepSeek访问超时。
### 解决
最初误以为是网络问题，但发现draft.py访问deepseek不会超时，仅refine访问超时，后意识到是连续三次串行调用导致访问超时累积。随后加入tenacity工具，设置指数退避功能，并开启deepseek支持的流式输入与流式输出，让大模型可以一边读一边写。
### 效果
成功实现访问不超时。

## 2026/5/7
### 事件
实现agent文件及main文件，并通过测试。

## 2026/5/8
### 事件
实现前端页面。
### 问题
前端页面的流程节点仅能显示流程开始，且在文章输出后直接显示剩余全部节点，无中间节点展示。
### 解决
后端发送“带event类型的标准sse”，前端按照event块解析，避免前端自行猜测分类导致的不稳定问题。

## 2026/5/9
### 事件
正在开发前端的流式输出功能。
### 问题
已实现后端控制台的流式输出，但前端仍为整块输出。
### 解决
意识到此前误将stream_console参数控制的控制台流式输出，当作可迁移到前端的流式输出，实际该参数仅能控制控制台流式展示，无法作用于前端。因此需专门编写stream参数，以实现前端的流式输出。